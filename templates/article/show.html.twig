
{% set article = hookable_metadata.context.article %}

<div class="page-body">
    <div class="container-xl">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">{{ article.title|nl2br }}</h5>
                        <h6 class="card-subtitle mb-2 text-muted">Price: <strong>{{ article.price|nl2br }}</strong></h6>
                        <p class="card-text">Brand: <strong>{{ article.brand|nl2br }}</strong></p>
                        <p class="card-text">Ref Supplier: <strong>{{ article.referenceSupplier|nl2br }}</strong></p>
                        <p class="card-text">Description : <strong>{{ article.description|nl2br }}</strong></p>
                        <p class="card-text">Category : <strong>{{ article.category.title|nl2br }}</strong></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

